/** Reviews user-authored work-authorisation wording without providing legal advice. */
import {NextResponse,type NextRequest} from "next/server";import{z}from"zod";import{getRequestUser}from"../../../../lib/api-auth";import{assertAiRouteRateLimit,RateLimitError,reviewWorkAuthorisationWithOpenAI}from"../../../../lib/openai-server";import{assertCanUseAi,FeatureGateError,trackAiCall}from"../../../../lib/feature-gate";
const schema=z.object({category:z.enum(["eu_eea_swiss_citizen","existing_permission","sponsorship_required","country_specific","unsure"]),statement:z.string().trim().min(10).max(2000),targetCountries:z.array(z.string().trim().min(1)).max(20)});
export async function POST(request:NextRequest){try{const{user}=await getRequestUser(request);if(!user)return NextResponse.json({data:null,error:"Unauthorised"},{status:401});const body=schema.parse(await request.json());await assertAiRouteRateLimit(user.id);await assertCanUseAi(user.id);const result=await reviewWorkAuthorisationWithOpenAI(body);await trackAiCall(user.id,{feature:"work-authorisation-wording",model:result.model,promptTokens:result.promptTokens,completionTokens:result.completionTokens,costUsd:result.costUsd});return NextResponse.json({data:result.value,error:null});}catch(error){const status=error instanceof z.ZodError?400:error instanceof FeatureGateError?402:error instanceof RateLimitError?429:500;return NextResponse.json({data:null,error:error instanceof Error?error.message:"Review failed"},{status});}}
