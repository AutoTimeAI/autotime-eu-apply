/**
 * /dashboard/cv-tailor — the CV/resume tailoring workspace. Renders
 * `CVWorkspace`, which lets the user adapt their CV to a specific job.
 */
import CVWorkspace from "../../../components/cv/CVWorkspace";
export default function DashboardCvTailorPage() { return <CVWorkspace />; }
