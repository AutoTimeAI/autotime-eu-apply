/**
 * /dashboard/follow-ups — outreach/follow-up tracking workspace. Renders
 * `OutreachWorkspace`, which manages follow-up messages and their status.
 */
import OutreachWorkspace from "../../../components/OutreachWorkspace";
export default function DashboardFollowUpsPage() { return <OutreachWorkspace />; }
