# Pre-Release Report — 2026-09-19 — `273040a2`

Prepared per `docs/reference/claude-code-pre-release-runbook.md`. Verification only — no deploy, migration, env-var change, or push was performed as part of this pass.

## Release candidate

- **Branch:** `main`
- **Release SHA:** `273040a23a081d0e0417a6394edfe7fcfcfeec61`
- **Comparison base (last deployed production commit):** `74c07901cb077c602ede116e56e7131d7c015f1e` (currently live at `dpl_2UWL9i3rYx6kzeGRFkrmFCXxKADd`, confirmed `isRollbackCandidate: true`)
- **Ancestor check:** `273040a2` is on `main`; local `main` == `origin/main` at the time of this report (`git fetch` + `git rev-parse` both returned `273040a2...`)
- **Uncommitted local state (preserved, not touched):** modified screenshot/visual-snapshot binaries, `apps/web/next-env.d.ts` (generated), and two untracked Chrome Web Store screenshot PNGs at repo root — none are code changes

## Change summary since last deployed commit (`74c07901` → `273040a2`)

| Commit | Files | Classification |
|---|---|---|
| `cacdf66f` | `supabase/migrations/20260919190000_rls_initplan_and_duplicate_policy_hardening.sql` | Database (RLS policy rewrite + duplicate-policy cleanup) |
| `d80727fb` | `docs/quality-assurance.md`, `docs/reports/beta-waitlist-release-checklist-2026-09-19.md` | Documentation-only |
| `273040a2` | `docs/reference/claude-code-pre-release-runbook.md` | Documentation-only |

**No application code, dependency, or configuration changes** since the currently-live deployment. The one database migration in this range (`20260919190000`) was already applied directly to production via the Supabase SQL editor and independently re-verified live via `pg_policies` before this report (see `docs/quality-assurance.md`, "RLS initplan + duplicate-policy migration applied and verified"). It only rewrites existing policy expressions (`auth.uid()` → `(select auth.uid())`) and drops 3 policies that duplicated an existing `ALL` policy on `custom_job_sources` — same access rules, no behavioural change, already confirmed live.

Because there is no application-code delta, a fresh production deploy of `273040a2` would serve **functionally identical** behavior to what's already live at `74c07901` — the only difference is the two doc files and the migration, which is DB-side and already applied independently of the app deploy.

## Required automated gates

| Gate | Command | Result |
|---|---|---|
| Typecheck | `pnpm typecheck` | ✅ PASS |
| Lint | `pnpm lint` | ✅ PASS |
| Unit suite | `pnpm test:unit` | ✅ PASS |
| Mobility suite | `pnpm test:mobility-suite` | ✅ PASS |
| Production build | `pnpm build:web` | ✅ PASS (`/waitlist` route present, confirming the beta-gate feature is in the build) |
| Production hardening | `pnpm test:production-hardening` | ✅ PASS |
| Manual production deploy checks | `pnpm test:manual-production-deploy` | ✅ PASS |
| Repository organization | `pnpm test:repository-organization` | ✅ PASS |

All 8 required gates green.

## Conditional gates

- **Extension code changed?** No — no `apps/extension/**` changes since last release. Skipped.
- **Migrations changed?** Yes — see above. Verified: idempotent (`alter policy`, `drop policy if exists`), no ordering dependency on other pending migrations, already applied to and verified against the live production database independently of this app deployment, compatible with the currently deployed application code (pure RLS-policy rewrite, no schema/contract change). Not re-applied here.
- **Auth/RLS/SECURITY DEFINER/admin/billing/AI-metering/secrets changed?** Yes, earlier in this session (not in this delta specifically, but as part of today's broader work) — full security-advisor review already performed and documented in `docs/quality-assurance.md`: two real RPC-exposure gaps found and fixed (`increment_ai_rate_limit`, two `auth.users` trigger functions), both re-verified via advisor re-scan showing the findings cleared. RLS policy rewrite in this delta re-verified live via direct `pg_policies` query.
- **User-visible flows changed?** No app code changes in this delta. The beta-waitlist flow itself was already verified live end-to-end earlier today (real QA session, real job paste → analysis → application-gate → admin-denial walkthrough against production) and documented in the same file.
- **Dependencies changed?** No — confirmed via `git diff --stat` on `package.json`/`pnpm-lock.yaml`/workspace package.json files between the last deployed commit and this one: no diff.

## Release safety checks

- **Workflow input validation:** Confirmed `.github/workflows/production-deploy.yml` requires `commit_sha` matching `^[0-9a-f]{40}$` and `confirmation` exactly equal to `DEPLOY PRODUCTION`, and verifies `git merge-base --is-ancestor` before checkout.
- **Rollback capture:** Confirmed the workflow captures the current READY production deployment's URL *before* deploying (`Capture current production deployment for rollback` step) specifically so a failed smoke check has something concrete to restore to.
- **Build + smoke + rollback:** Confirmed the workflow runs `vercel build --prod`, deploys, then runs `pnpm smoke:web` against the new deployment URL, and automatically rolls back to the captured previous deployment on any prior step failure (`Roll back to previous production deployment` step, conditioned on `failure()` and a non-empty previous URL).
- **Credentials/config presence:** `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID` are referenced from the `production` GitHub environment's secrets/workflow env — presence not directly inspectable from this pass (metadata-only check per runbook instruction; no secret values displayed or requested).
- **Smoke test coverage:** Confirmed `scripts/smoke-web-dashboard.mjs` checks (a) the public root path returns 2xx, `text/html`, and expected Private-Beta-v1 HTML markers, and (b) an unauthenticated request to `/dashboard` returns a 3xx redirect (proving the backend and auth gate are both reachable and functioning) — satisfies "public HTML + unauthenticated protected route."
- **Rollback target:** Identified — current live production deployment is `dpl_2UWL9i3rYx6kzeGRFkrmFCXxKADd` (commit `74c07901`), confirmed via the Vercel API as `state: READY`, `isRollbackCandidate: true`.

## Unresolved warnings / deferred non-blockers

- **Sentry source-map upload:** requires a valid build-time auth token; a missing token does not currently fail the Vercel build. Not verified as present/absent in this pass (would require inspecting build-time secrets, out of scope for a metadata-only check). Flagged as an existing observability gap per the runbook's standing note — **accepted non-blocking**, not new to this release.
- **Supabase Auth "leaked password protection"** remains disabled (dashboard-only toggle, not SQL-fixable) — **deferred**, tracked in `docs/reports/beta-waitlist-release-checklist-2026-09-19.md`, non-blocking.
- **76 unindexed-FK + 31 unused-index performance-advisor findings** — INFO-level, **deferred** to a post-launch pass driven by real query-log data, non-blocking.
- **Chrome extension review** — separate, independent track (Chrome Web Store review pending as of this report), does not gate this web-app release.

## Rollback plan

If the next production deploy fails its post-deploy smoke check, the workflow automatically rolls back to `https://autotime-eu-apply-3lcpiyjtu-rajs-projects-6830d68b.vercel.app` (the current live `74c07901` build) with no manual action required. If a defect surfaces post-deploy that the smoke test doesn't catch, the same URL is the manual rollback target via `vercel rollback <url> --token=... --prod`.

## Decision: **GO**

All 8 required gates pass, all applicable conditional gates were reviewed and satisfied (or correctly marked N/A), the one database change in this delta is already applied and independently re-verified live, there is no application-code delta versus the currently-live build, a valid rollback target exists, and no unresolved release-blocking risk was found. All warnings above are pre-existing, already-classified, and non-blocking.

**Deployment command for explicit execution after approval (not run by this report):**

```
gh workflow run production-deploy.yml -f commit_sha=273040a23a081d0e0417a6394edfe7fcfcfeec61 -f confirmation="DEPLOY PRODUCTION"
```

This report is the artifact confirming release-readiness. It is not authorization to deploy — that requires a separate, explicit instruction.
